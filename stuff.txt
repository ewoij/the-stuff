Goal: CRUD app to manage projects tasks

data structure proposal:
 - projects
     - id
     - name
     - content
     - created
     - updated
 - tasks
     - id
     - project_id
     - parent_task_id
     - branch
     - PR
     - title
     - content
     - created
     - updated
 - task_status (event sourcing with history)
     - id
     - task_id
     - status: TODO, PROGRESS, DONE, ARCHIVED
     - created
 - task_comments
     - id
     - task_id
     - content
     - created
     - updated

features:
 - API a local claude code agent can use CRUD tasks
     - important: fetch next task
         - lock
         - set to in progress
         - return the task as json

UI:
 - projects
     - CRUD
 - tasks
     - select project
     - columns style
